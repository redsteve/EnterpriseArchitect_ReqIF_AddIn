This small project is an Add-In for the modeling tool SparxSystems Enterprise Architect (EA). The goal ist to enable the EA to import requirements from a Requirements Interchange Format (ReqIF) file, resp. to export requirements to a ReqIF file. The Requirements Interchange Format is a standardized XML-based format for requirements interchange between organizations or tools.

The Add-In is developed using C#/.NET and #develop (Sharp Develop).
